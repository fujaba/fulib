# --------------- General Settings ---------------

language: java
jdk:
  - oraclejdk8
# TODO unsupported due to JavaFX
#  - openjdk8
#  - openjdk11

# --------------- Gradle Cache ---------------

before_cache:
  - rm -f  $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/

cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/
