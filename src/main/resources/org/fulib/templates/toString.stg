toString(names, superClass) ::= <<
   @Override
   public String toString()
   {
      final StringBuilder result = new StringBuilder(<if(superClass)>super.toString()<endif>);
      <names:attrLine(); separator="\n">
      return result.<if(superClass)>toString()<else>substring(1)<endif>;
   }
>>

attrLine(name) ::= <<
result.append(' ').append(this.get<name; format="cap">());
>>
