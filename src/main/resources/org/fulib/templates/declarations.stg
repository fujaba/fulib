packageDecl(packageName) ::= <<
package <packageName>;
>>

importDecl(qualifiedName, static) ::= <<
import <if(static)>static <endif><qualifiedName>;
>>

classDecl(clazz) ::= <<
public class <clazz.name><if(clazz.superClass)> extends <clazz.superClass.name><endif>
{
>>
