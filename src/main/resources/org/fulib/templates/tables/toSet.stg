toSet(className) ::= <<
   public Set\<<className>\> toSet()
   {
      int column = this.getColumn();
      Set\<<className>\> result = new LinkedHashSet\<>(this.table.size());
      for (List\<?> row : this.table)
      {
         <className> value = (<className>) row.get(column);
         result.add(value);
      }
      return result;
   }
>>
